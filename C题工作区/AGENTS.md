# 项目绘图约定

- 用户明确要求：以后在本项目做图或重做图时，使用 `modelviz-skill`。
- 先读取 `/Users/justingao/.codex/skills/modelviz-skill/SKILL.md`，执行模板候选召回、真实数据适配、绘图与技术/视觉检查。
- 图片、绘图代码、输入副本、模板选择、依赖及质检记录均保存在本工作区，禁止写到 tmp。
- 不改动原始或清洗数据，不移动时间键，不使用平滑曲线替代原始调度点。时间映射未确认时标明内部工作假设。
- 同时提供 300 dpi PNG 与 SVG；图内全部使用英文（标题、坐标轴、图例、注释、脚注、月份），不得出现中文；保留明确单位及一致配色，实际查看图片后再确认版式合格。
- 保留其他成员工作及已冻结交付包。新图用独立版本目录，并同步当前论文/报告引用和来源哈希。

当前版本：`reports/figures/modelviz_v3_en/`；可复现命令见 `reports/modelviz_v3_en_revision.md`。
