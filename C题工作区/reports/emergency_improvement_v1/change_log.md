# 本轮执行变更与失败保留

- 用户提供的执行文件已授权直接研究/实现/验证及常规选择，故采用其批准的路线，不重复询问设计许可；技能默认要求的 commit 与目录位置由用户“不提交、独立目录”覆盖。
- 原始预测/规划/执行函数精确 AST 抽取，避免导入原模块时写入旧 data/interim 缓存。B0/B1 已逐段零差异复现，未使用次级择优规则、换求解器或重新选模型。
- `--policies` 仅用于独立验收阶段执行已经声明的子集，不增加策略或改参数。
- PE 的未来扰动软件集成检查与另外四种策略一起执行，早于 P/E 一月独立验收；这不是候选经济选优。PE 的正式一月及年度经济回放仍以独立门槛为前置条件。提前执行的测试原输出保留。
- 首轮7项单元测试因新策略模块尚未实现而失败（预期 red）；实现后7项通过（green），日志均保留。
- agent-reach Exa 网络入口出现 DNS 失败，改为直接核查 MIT/FPP3 原始网页资料。未增用 Q3 预报或未核实社交数字。

后续若发现实现缺陷，追加原因、受影响产物和复跑范围，不改原冻结协议来掩盖结果。

- 全年主方案独立检查与敏感性产物写入短暂并发，中间 `independent_validation_runs.json` 对尚未写完的B1_terminal目录记录FAIL（缺五个文件），其余已完成主策略通过。待18条路径全部完成后，最终 `independent_validation.json` 全量通过。中间时机失败报告保留，不当作模型失败或掩盖为当时已完成。
- 对照分析表将终态/效率/功率分支的节省列明确配对到同设定B0/B1，并新增引用策略名。此举只修正比较标签/对象，未改变任何回放结果。
- modelviz月度图的初版脚注“common scales”可能被误读为左右轴范围完全相同，已改为只声明右侧节省轴共用尺度；初版脚本和PNG、修复原因均保留。
- 论文终审建议将“重算冻结计划”改为“核对冻结采购一致性，复算计划与实际约束”，并明确独立验证器没有再次重优化MILP；已修改，数值与代码不变。
- 打包前发现总复现入口先调用一月独立门槛、后生成其必需的因果证据，新工作区会因此中断。已将因果软件检查移到一月B0/B1/P/E之后、独立门槛之前；正式PE回放仍在门槛之后。保留缺失证据导致失败的负对照，并在隔离工作区实际重跑四条一月路径、五策略未来扰动测试及独立门槛，全部通过，见 `reproduction_dependency_validation.json` 与原始 `reproduction_january_gate.json`。独立审查另以AST检查新顺序、拒绝旧顺序。此修复不改变主实验18条结果，也不冒称又重跑了18条全年优化。
