# Independent bounded code review

Scope: read-only review of `inputs.py`, `policy.py`, `run.py`, `extra_checks.py` and `reproduce.py` against the frozen protocol and validation brief. The imported `compat.py`, `kernel.py` and scaling tests were inspected where necessary. No production code was edited and no already-passed annual checks were rerun. This report is not final artifact acceptance.

No defect was identified in the inspected production paths that would invalidate the annual source selection, held commitments, physical execution or A-rule billing. Three test-coverage gaps were raised; the owner has accepted all three additions. Their status below distinguishes observed code from pending execution.

## Findings and disposition

| Finding | Evidence and consequence | Disposition / remaining evidence |
|---|---|---|
| P2 — Explicitly retain an OLS positive control | [extra_checks.py:68](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/extra_checks.py:68) originally selected only the three winning policies plus `q42_fixed`. If Q42 and Q43 both selected fixed-price controls, every actual-price perturbation could be classified as irrelevant at lines 90–93, leaving no post-availability price response test. | **Addressed in code, execution pending.** The current line 68 explicitly includes `q42_ols`; this was read back. Require a nonzero post-availability plan response for its actual-price mutation and unchanged pre-availability evidence. |
| P2 — The Q42 PV-publication negative control was a no-op | [extra_checks.py:43](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/extra_checks.py:43) only reads or changes PV-publication data when `with_pv=True`. For Q42, original and `pv_release` constructors therefore receive identical relevant inputs. Their equality alone would not catch an accidental unconditional Appendix 3 read introduced later. The production guard in [inputs.py:30](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/inputs.py:30) is correct on inspection. | **Addressed by the agreed test addition, implementation/execution pending at review cutoff.** The owner will run Q42 construction and full execution under a forbidden-file-read guard covering Appendix 3 and its processed publication files. Require successful Q42 execution with the guard active, plus a guard positive control that proves a forbidden read is detected. |
| P2 — Pre-availability evidence compared procurement, not all available inputs | [extra_checks.py:82](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/extra_checks.py:82) through line 89 compares the full candidate `q` array before availability. The user separately requires available information to remain unchanged; stable procurement alone does not establish that forecast/scenario/price inputs stayed unchanged. | **Addressed by the agreed test addition, implementation/execution pending at review cutoff.** The owner will compare all numeric pre-availability evidence arrays and initial energy, retaining separate energy/cash tolerances and exact discrete fields. Include scenario dates, release stage and cutoff metadata where claimed. |

No annual policy parameter or production calculation change is required for these findings. They concern the strength of the additional causal evidence.

## Production checks by inspection

- [inputs.py:33](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/inputs.py:33) restricts calibration observations to the publication's own calendar day. Lines 46–52 apply the April 1 activation, matching issue hour, trailing 28-day targets, completed-by-midnight cutoff and positive-raw daylight mask. Historical forecasts at lines 74–80 use each historical date's own correction call; no current-date bias is retrospectively imposed on historical residuals.
- [inputs.py:65](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/inputs.py:65) freezes B0 load, matches the source release stage, uses actual completed dates in the 112-day calendar window, and derives the 28-day initializer subset. Q42's production constructor uses `with_pv=False` in [run.py:35](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/run.py:35), and its scenario branch uses B0 PV/residuals.
- [run.py:44](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/run.py:44) copies the remaining midnight reference and currently held commitments before selecting a replacement. [policy.py:70](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/policy.py:70) appends the held vector as the seventh candidate and evaluates it under the same current objective. Only future slots are replaced; the original midnight vector remains fixed.
- [policy.py:16](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/policy.py:16) adds the A-rule contract correction to both score and gradient. The optimizer uses `q=1000*x`, `J/10000`, and gradient factor `.1`. All six/seven candidates are rescored and the first score within `1e-8` of the minimum is selected. Initializer terminal/nominal constraints remain confined to the MILP starting point.
- [run.py:63](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/run.py:63) advances the policy's actual state without daily reset. Lines 69–74 settle Q4 at actual target prices, apply the final commitment relative to midnight exactly once, charge full `5p` emergency cost and retain paid-unused ordinary commitments. The inventory proxy appears only in a separately named summary diagnostic.

## Perturbation and reproduction boundaries

The availability split in [extra_checks.py:85](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/extra_checks.py:85) is correct: a publication issued exactly at the cutoff is available then; actual observations starting at the cutoff are not. The mutation builder bypasses cached prices and rebuilds affected B0 forecasts only after completed history becomes available. The July 15 full-day freeze assertion for non-updating policies is appropriate. These remain three-day counterfactual implementation checks, not general causality proofs or new annual economic comparisons.

[reproduce.py:21](/Users/justingao/Documents/CUMCM/C题工作区/scripts/unified_direct_v5/reproduce.py:21) requires a fresh descendant of the v5 result root. It copies source/data, verifies copied hashes, rebuilds price forecasts, and invokes the copied annual runner with an isolated runtime and disabled bytecode writes. The C binary is rebuilt in that workspace. Ledger, plan versions, daily tables, every original decision's candidate arrays and aggregate fees are compared. The permanent comparison report is confined to v5 and is not overwritten if already present. This is same-host/dependency reproduction; it is not cross-platform verification. Full reproduction execution and final artifact acceptance remain pending outside this bounded review.

No legacy write is reachable through the reviewed production/test/reproduction entry points. The copied `compat.solve_target_model` still contains old diagnostic paths guarded by `tag`, but all reviewed callers use its default `tag=None`; those dormant branches are not evidence of an executed legacy write. Final protected-file SHA comparison remains necessary and is owned by the root task.

Prior numerical and Q3 annual validation reports were read as existing evidence, not rerun here. The independent Q3 snapshot is `independent_q3_result.json`; the final ten-policy independent audit, executed amended causal checks, selected-policy reproductions, workbook readback and protected-file hashes must be attached separately before complete acceptance.
