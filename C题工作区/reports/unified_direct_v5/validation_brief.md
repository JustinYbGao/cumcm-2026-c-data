# Independent validation task

All work is in `/Users/justingao/Documents/CUMCM/C题工作区`. Own only `scripts/unified_direct_v5/independent_*.py` and `reports/unified_direct_v5/independent*`. You are not alone; do not revert others. No commit/push, no tmp or legacy writes. Read complete original problem and relevant frozen audit/source records. Write a NEW independent validator, never import production C/kernel/greedy/billing functions.

Scope: independent source reconstruction and full new-policy physical, billing, information and candidate audit. Root builds production and runs experiments. First reconstruct B0 forecasts from actual prefixes, Appendix3 interpolation/release data and causal PV correction, and OLS prices; inspect existing preprocessing code to find raw workbook mapping. Report source risks early. Use `.venv/bin/python` with `PYTHONDONTWRITEBYTECODE=1`, cache/tmp under new runtime.

Production contract (root will implement): `results/unified_direct_v5/runs/<policy>/ledger.csv`, `daily.csv`, `summary.json`, `plan_versions.csv`, `decisions.json` list, and `evidence/YYYY-MM-DD_HH.npz` for every decision. NPZ keys: q [K,T], scenario_net [S,T], price [T], load_hat/pv_hat/raw_pv [T], q0 (empty midnight else remaining midnight q), current (empty midnight else held commitments), risk_delta [T], score [K], ordinary [K], emergency_fee/end_energy [K,S], selected_index scalar. Metadata: date, issue_hour, source_hour, correction bool, price_method, initial_energy_kwh, scenario_source_dates, scenario_count, selected_index, optimizer logs (two), milp_initializer, evidence_file. K=6 midnight; K=7 intraday with keep_current last; labels profile_start/incumbent/final, point_start/incumbent/final, keep_current. Tie first score <= min+1e-8.

Ledger same old Q3 columns: date,slot_id,interval_start/end,load/pv_actual_kwh,grid_original_kwh,grid_effective_kwh,price_yuan_per_kwh,charge/discharge_actual_kwh,energy_start/end_actual_kwh,emergency_kwh,surplus_kwh,unused_grid_kwh,pv_curtailment_kwh,original_cost_yuan,increase_cost_yuan,decrease_adjustment_yuan,contract_cost_yuan,emergency_cost_yuan,total_cost_yuan. Versions rows for all remaining slots at each 00/06/12/18, q named grid_kwh and original grid_original_kwh. Scenarios use actual completed dates within prior 112 calendar days, date>=Jan2, current net forecast + (historical actual net minus historical issued net forecast), source stage 0 for no-update/SOC feedback, own hour for PV updating. Historical corrected forecasts use own historical cutoff and activation date Apr1, never today's correction parameter. B0 load frozen midnight. PV bias uses same-day published horizons, raw>0, same issue hour, previous28days interval_start>=cut-28d and interval_end<=midnight cut; no current-day obs. Raw before Apr1. Q42 no Appendix3 dependence uses B0 PV. Price forecast identical causal OLS 28d K2 lag1/7 weekday floor1e-6, refreshed allowed hours. mu=.38232 kappa5; target score sum(p*q) or sum(F_A(q;q0,p)) + mean(emergency_fee-mu*(end-initial)). Physics E1200..10800, eta .9 each, P5000/6. emergency5p, keep full paid unused commitment.

Validate all candidate objectives (not only selected), q selection and optimizer trace minima, source dates/cutoffs/scenario counts, all48096intervals per policy, crossday, updates and cost aggregation to1e-6kWh/1e-5yuan. Optimize independent numpy implementation, no production imports. Include D112 original ledger regression to13913892.481868185 and Q1 to35126.94858928963. Separate XLSX readback will be another task; you may leave workbook audit to it but integrate reported coverage honestly. Send root concise source/interface findings early, then implement full validator and report. Final output file `reports/unified_direct_v5/independent_report.md` plus JSON numeric metrics and failures. Do not mark unfinished checks PASS.
