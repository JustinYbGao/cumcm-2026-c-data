# Result workbook exports

The five workbooks retain source numeric precision. Excel displays energy and fee values to six decimals, so copied display text can lose digits even though the stored cell values remain unrounded. `policy_mapping.json` records the selected strategies, sources, fees, assumptions, and hashes.
